# Cancer spectrum maps

`plot_cancers_3d_maps.py` requires the local processed-spectra table plus the generated Korean Cancer Association cohort manifest and PCA scores. Those inputs are review data kept outside Git.

The script generates manuscript PNG, TIFF, and PDF figures plus an aggregate subject-count CSV in this directory. Generated files are excluded from Git and stored with the publication deliverables in SharePoint.
