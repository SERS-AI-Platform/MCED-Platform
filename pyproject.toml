[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sers-analysis"
version = "0.1.0"
description = "SERS spectral analysis pipeline for cancer screening"
readme = "README.md"
license = {text = "Proprietary"}
requires-python = ">=3.10"
authors = [
    {name = "SOLUM Healthcare"}
]
keywords = ["SERS", "spectroscopy", "cancer screening", "metabolomics"]
classifiers = [
    "Private :: Do Not Upload",
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "pyyaml>=6.0",
    "scikit-learn>=1.3",
    "click>=8.1",
    "tqdm>=4.65",
    "openpyxl>=3.1",
    "pydantic>=2.10",
    "psycopg2-binary>=2.9",
    "typer>=0.20",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "ruff",
    "mypy",
    # CI installs only "[dev]" (no permission to touch .github/workflows/ci.yml
    # from this token to switch it to "[all]"), but the test suite imports
    # viz/api/ml packages - pull those extras in transitively so collection
    # doesn't fail on ModuleNotFoundError.
    "sers-analysis[api,ml,viz]",
]
ml = [
    "xgboost",
    "lightgbm",
    "torch>=2.0",
    "pywavelets>=1.4",
]
viz = [
    "matplotlib>=3.7",
    "seaborn",
    "pillow>=10.0",
]
mlops = [
    "mlflow>=3.1,<4",
]
api = [
    "fastapi>=0.135,<1",
    "httpx>=0.28,<1",
    "uvicorn[standard]>=0.42,<1",
]
notebook = [
    "ipykernel>=6.29,<8",
    "nbconvert>=7.16,<8",
]
all = [
    "sers-analysis[api,dev,ml,viz,mlops,notebook]",
]

[project.scripts]
sers = "sers.cli:cli"

[project.urls]
Homepage = "https://github.com/SERS-AI-Platform/MCED-Platform"
Documentation = "https://github.com/SERS-AI-Platform/MCED-Platform#readme"
Repository = "https://github.com/SERS-AI-Platform/MCED-Platform.git"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
# Pragmatic ruleset for a research codebase (relaxed 2026-06):
# keep real-bug (F), import-order (I), whitespace (W) and core style (E);
# drop docstring (D) and naming (N) enforcement; ignore noisy, unfixable
# style rules. Tighten over time as the team matures.
select = ["E", "F", "I", "W"]
ignore = ["E501", "E402", "E701", "E722"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
markers = [
    "slow: marks tests that need real files or are slow (deselect with '-m \"not slow\"')",
]

[tool.mypy]
# No hardcoded python_version: CI runs mypy once per matrix leg (3.10/3.11/3.12).
# A fixed "3.10" here made mypy analyze all three under 3.10 semantics, which
# broke on newer numpy stub syntax (PEP 695 `type` statements) that mypy
# refuses to parse below 3.12. Letting mypy default to the running
# interpreter's own version keeps each matrix leg checked against its own
# Python version instead.
files = [
    "src/sers/config.py",
    "src/sers/scoring.py",
    "src/sers/cli",
    "src/sers/master_data",
    "src/sers/mlflow_tracking.py",
]
mypy_path = "src"
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
disable_error_code = ["import-untyped"]

[[tool.mypy.overrides]]
module = "mlflow"
ignore_missing_imports = true
